{
  "name": "linked-in-trace",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.7",
    "jsdom": "^30.0.1",
    "vitest": "^3.0.0"
  }
}
